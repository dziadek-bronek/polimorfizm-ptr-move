cleaning dlHandle powstale po wczytaniu  soChild->CConfigChild
add static_cast and dynamic cast


najpierw unit testy!!!


CAdderChildCreatore - zmodyfikowanie tak, by jako Creator byl dostosowany do 


UML - nauka - dokumentacja mojego frameworka

zmiany nazw

Dokumentacja readme.md



Jest: tworzenie selectora w configuratorze i zwracanie selectora
Zamiast tego: configurator zwraca selectorCoreVoidPtr a w CFramework wywolanie CChildSelector::createNew(selectorCoreVoidPtr) ktory tworzy odpowiedni selector: najpierw rozpoznaje jakiego typu jest selectorCoreVoidPtr rozpoznaje przy pomocy dynamic cast)
inna opcja: configurator zwraca dwuelementowa strukture (typ i wartosc selectorCoreVoidPtr) z typem - niech to bedzie strukctura z dwoma polami, a CChildSelector::createNew tworzy 



Testy jednostkowe ktore znajda blad
wyciagniecie dlHandle na zewnatrz wartosc przechowywana w zmiennej globalnej - i usuwanie jej na koncu w sekcji catch albo po niej


write UT-s for class only dedicated each test needs only file <CClassName>.o from original sources.


Ctrl-b then w -- interesting effect
czy mozna zrobic mock metody ktora jest static? To rozwiazywaloby akrobacje z klasa CChecker


create UTs based on example1

check carefully
	where is used framework->configAction(222
		using CConfigChild
	  	tutaj mozna zrobic tak, ze
			class CAddConfig(number - 222, map, creator) 
			tez ze configChild ma pole w ktorym przechowuje mape, czyli configChild->action
	and where is used selectorConfigurator->action

Registering child creator with given event: check for existing events and prevent from assignment

to change names of files
to construct library with headers exposed
commands for build lib and executable, and commands to build and run tests: to be added to files or to CMakeLists.txt

========================
to format code -- add script to repository

# to format:
clang-format -i -style=chromium ~/cprogs/github/polimorfizm-ptr-move/src/*.cpp
clang-format -i -style=chromium ~/cprogs/github/polimorfizm-ptr-move/src/include/*.hpp
clang-format -i -style=chromium ~/cprogs/github/polimorfizm-ptr-move/uts/src/*.cpp
========================


configurator for factory - no exit option, can be configured as (is a list of child itself configured by vector and by 
factory::ADD_CONFIGURATOR_OPTION

configure configurator for selector -- operation configurator::ADD_FACTORY_OPTION selection option to selector
selector


uts - two unit tests;
make tests1; make tests2;
./ut1
./ut2
all




to produce simple units tests for CChild1 constructor and destructor

build-and-run, CMakeLists -- separate git submodule
format - separate git submodule
unit tests  - separate git submodule

change name: worker(instead child), workerIf(instead parent), workerSelector, workerCreator,

factory, selector
Create library 'framework', library implementation.


idea



old:
integrate child-creator.hpp into children.hpp, and child-creator.cpp into children.cpp


int  main() {

	for(int i = 0; i < 10; ++i) {
		input = 1, 2, 
		switch(input) {
			framework.register(1) // Child no, event no is auto starting from 1
			framework.register(2)
			framework.register(3)
		}
		run framework
	}
}


========================

tmux:
0 - text editor, modify program files
1 - buffer
2 - build


uwaga (0 moze byc zamienione z 2 bo nie pamietam jak to bylo)
3 - git
========================



